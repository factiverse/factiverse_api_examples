# Javascript authentication

1. Add your domain and client ID in auth_config.json.

2. Run `npm install`, `npm start` and navigate to `localhost:4000` in your browser for a demo login.

You can also follow the steps in this Auth0 tutorial: https://auth0.com/docs/quickstart/spa/vanillajs/01-login
